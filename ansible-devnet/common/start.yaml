- name: Copy start script on Servers
  copy:
    src: "{{ playbook_dir }}/scripts/{{ file_name }}"
    dest: "/home/ubuntu/{{ file_name }}"
    mode: "0755"
    force: yes
  become: yes

- name: Run start script on servers
  shell: "/home/ubuntu/{{ file_name }}"
  become: yes
  become_user: ubuntu
